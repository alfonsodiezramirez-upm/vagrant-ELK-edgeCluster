apiVersion: v1
kind: Service
metadata:
  name: myloadbalancer
  labels:
    app: kubernetes-dashboard
spec:
  type: LoadBalancer
  ports:
  - port: 8001
    name: http
    targetPort: 8001
  selector:
    app: mydeployment